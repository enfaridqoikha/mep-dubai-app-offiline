name: mep_dubai_app
description: Offline DEWA-style MEP quiz with dynamic themes (Dubai Building Code 2021)
publish_to: "none"
version: 1.1.0+3

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.6
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/questions/
